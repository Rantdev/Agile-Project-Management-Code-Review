[phases.setup]
nixPkgs = ["nodejs_18", "python311", "make", "gcc"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["cd server && npm run build || echo 'No build script'"]

[start]
cmd = "cd server && node server.js"
